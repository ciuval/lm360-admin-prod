import { MatchProgress } from '../components/MatchProgress';

<MatchProgress profile={{
  name: user.name,
  photo: user.photo,
  bio: user.bio,
  interests: user.interests,
  matchScore: user.matchScore
}} />
